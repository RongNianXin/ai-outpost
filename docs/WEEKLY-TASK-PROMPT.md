# 周六内容准备任务现行提示词

2026-09-07用户授权恢复既有任务；正文与本机任务同步。任务绑定不在共享仓库保存真实ID；修改本文后须按明确授权同步实际自动任务并回读。

每周六10:00（Asia/Shanghai）在当前绑定的AI前哨站任务执行本地周刊准备。先读取AGENTS.md、task_plan.md当前节和progress/findings相关最新记录，核验自己仍为现任写入者；身份不一致仅报告并停止生产，不迁移或新建调度。
开始任何新闻检索前运行pnpm.cmd content:research:start，读完实际输出的docs/source-library/README.md和catalog.json，输出截断就分段补读；加载失败停止受影响检索。再读docs/CONTENT-OPS.md、docs/FACT-CHECK-PROTOCOL.md、docs/PROMOTION.md、content/sources.json、ops/weekly-run-state.json和最新期刊，运行pnpm.cmd ops:check。该检查只报告，不能自动改公开状态。运行模式paused/emergency_stop停止；draft_only为允许准备草稿。
默认覆盖上周六10:00至本周六10:00，记录计划截点、实际启动、逐源访问、补查时刻和失败入口，重叠检索按事件去重，晚启动明确扩展截点。任何新消息须在实际检索时刻前168小时内；只有日期时按不利边界处理，旧消息只作背景。先查官方最新发布、更新日志、会员套餐/权益和模型/API/订阅价格，再查研究/独立测评/媒体/社区线索；AI Agent为主、其他AI革新为辅。套餐名称与价格成对核对，区分新购、升级、续费、既有用户、地区和渠道。重大基础模型换代必须进入审查并按重要性纳入，不等点赞热度，不因已有厂商案例或卡片满额遗漏。官方承载论坛不等于用户帖子为官方；媒体/用户推荐不替代证据。热帖区分发布时间与最后回复时间，原始结论回公告、论文或实验，执行主体与文章双层敏感性审查，不作无污点保证。
先列最重要三件事再筛选。为每条分别记录可见讨论信号和实际重要性，热度不证明真实性。正式通常6条，其中1–2条以合格独立作者/机构原创内容为主体，纯观点最多1条；找不到合格内容报告缺口和短版/例外建议，不硬凑或静默全官方。事实/第三方结果/编辑判断分别归因，记录方法、环境、限制、已知利益关系，不冒充作者亲测。检查转载声明、素材许可与原始出处；不选时政，不评论国家、地区、政府或公职部门。标题须与证据中的主体、型号/套餐、价格、时间和范围一致，并按事实独立撰写：操作者的自然语言增补、口头纠错和聊天原话只用于修正事实，不能直接当标题文本。每条新闻至少一条来源，交稿前至少复核一次，来源索引顺序与情报卡顺序一致。卡片默认只展示「内容详情」和「造成的影响」两个版块，按新闻复杂度弹性撰写、原则上每个不超过约200字；行动参考和技术风险收进可展开区域；影响按当期内容只聚焦行业或个人一侧。按humanizer-zh与项目文本审校规则检查中文。

首轮信息源结束后冻结候选、证据、检索时刻、可见热度信号、拟用标题、结构化稿和派生Markdown，创建一个只读独立审查子Agent，严格按FACT-CHECK-PROTOCOL的八项门禁输出PASS/REVISE/REJECT及从高到低顺序。子Agent不取得中央状态写权、产品写权、调度权、远端权限或发布权限，不接收私密来源名单和无关内部评价；由当前总指挥转交必要材料并作最终确认。默认最多两轮，只有新证据或预先约定条件才允许第三轮，第三轮后结束本批。平台不支持或当轮无子Agent授权时只报告并停在候选阶段，不由执行者冒充独立审查。
通过正式issueNumber、publishedAt、status和运行记录决定期号，不能只凭内部文件名宣布某期已完成。2026-09-12编号统一后内部id与文件名等于对外期号issue-NNN。保留已有未完成草稿，先核对阶段；有待审稿仅补查并报告，不能覆盖。本周无待审稿且未完成时创建下一连续正式期号的草稿，草稿文件名用issue-NNN-draft且issueNumber为目标期号，title严格为AI 前哨站第 NNN 期；未分配期号或纯演练稿用draft-<日期>-<主题>、issueNumber=0。定稿后改为issue-NNN.json并同步运行记录。更新本周运行记录，失败记录阶段，保留已有成果。
生成本地官网和公众号完整交付，源码参考tools/wechat-full-002-reference仅适用002，不冒充通用生成器；缺通用支持按现有指南制作和核验本期完整稿。暂不自动准备小红书或增加平台。核对派生Markdown的卡片数量、热度顺序、标题、事实、限制、来源、链接及隐私，不得残留淘汰项。运行content:validate、content:check:links、typecheck、lint、test、build及ops:test等相关检查，按项目规则处理具体失败与访问缺口。用content:preview:url定位新草稿，启动本地预览并在当前Codex打开真实页面，给作者具体白话判断与人工步骤，用户审核期间保留预览。
首轮交稿前补查重大消息；交付后冻结稿件。正式发布前再查新增事件，只列原文、重要性、建议替换项与影响，等作者决定后更新并重新核验；不自动覆盖成稿，不继承旧版确认。发布前后、交接前后按资料库规则维护已用来源及工具指针；快照后修改须补最终快照。
此任务仅自动准备、人工发布。禁止Commit、Push、部署、触发远端工作流、公众号后台草稿/预览/发表、邮件发送、社媒写入、群发、陌生私信、付费、SEO扩展、新调度和跨项目写入。每次外部发布需作者当次精确授权。完成、失败或需作者行动时通知；无实质变化不重复提醒。不保证关机/退出后补跑或无人运行时主动通知。
